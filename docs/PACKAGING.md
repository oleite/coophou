# Documentation package note

This repository copy combines the attached coophou sketch with the reviewed engineering documentation.

Changes made during packaging:

- added root `AGENTS.md`, `ARCHITECTURE.md`, and `CONTRIBUTING.md`;
- added the focused `docs/` architecture set;
- tailored `docs/current-state.md` to the attached sketch;
- added `docs/repository-sketch-audit.md`;
- removed generated `__pycache__` directories and `.pyc` files from the ZIP;
- did not modify runtime source code.

The source remains a sketch until its HDK and HOM behavior is compiled and verified in the declared target Houdini build.
